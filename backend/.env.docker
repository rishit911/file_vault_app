# Backend docker overrides - safe defaults for local dev
PORT=8080
JWT_SECRET=dev_jwt_secret_change_me
BASE_URL=http://localhost:8080
FILE_BASE_PATH=/data/files

# DATABASE_URL not necessary here because docker-compose sets it, but you may override locally

###
# Note: Do NOT commit production secrets. This file is optional but helpful for local overrides.