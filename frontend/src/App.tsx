import React from 'react';

export default function App() {
  return (
    <div style={{padding: 24}}>
      <h1>file_vault_proj - File Vault (Dev)</h1>
      <p>Frontend placeholder. API base: {import.meta.env.VITE_API_BASE_URL}</p>
    </div>
  );
}